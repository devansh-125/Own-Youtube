# 🏗️ RAG Search Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND (React)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Navbar / SearchBar Component                            │   │
│  │  - Accepts user input                                    │   │
│  │  - Calls /api/v1/search/hybrid endpoint                 │   │
│  └──────────────────────┬───────────────────────────────────┘   │
│                         │                                        │
│                         │ POST /search/hybrid                    │
│                         │ {query: "text", limit: 10}             │
│                         ↓                                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  SearchResults Page                                      │   │
│  │  - Displays results                                      │   │
│  │  - Shows search type & count                             │   │
│  │  - Maps VideoCard components                             │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                   │
└───────────────────────────┬───────────────────────────────────────┘
                            │
                  AXIOS API Client
                            │
                            │
┌───────────────────────────↓───────────────────────────────────────┐
│                      BACKEND (Node.js/Express)                    │
├─────────────────────────────────────────────────────────────────┐─┤
│  API Routes                                                       │ │
│  ┌──────────────────────────────────────────────────────────┐   │ │
│  │ POST /api/v1/search/semantic                             │   │ │
│  │ POST /api/v1/search/keyword                              │   │ │
│  │ POST /api/v1/search/hybrid  ← RECOMMENDED                │   │ │
│  └──────────────────────┬───────────────────────────────────┘   │ │
│                         │                                        │ │
│                         ↓                                        │ │
│  ┌──────────────────────────────────────────────────────────┐   │ │
│  │  Search Controller (search.controller.js)                │   │ │
│  │  - Validates request                                     │   │ │
│  │  - Calls appropriate search service                      │   │ │
│  │  - Returns formatted response                            │   │ │
│  └──────────────────────┬───────────────────────────────────┘   │ │
│                         │                                        │ │
│                         ↓                                        │ │
│  ┌──────────────────────────────────────────────────────────┐   │ │
│  │  Search Service (search.service.js)                      │   │ │
│  │                                                          │   │ │
│  │  ┌────────────────────────────────────────────────────┐ │   │ │
│  │  │ Semantic Search (Using Gemini Embeddings)          │ │   │ │
│  │  │ 1. Generate embedding for query                    │ │   │ │
│  │  │ 2. Fetch all published videos                      │ │   │ │
│  │  │ 3. Generate embedding for each video               │ │   │ │
│  │  │ 4. Calculate cosine similarity                     │ │   │ │
│  │  │ 5. Return top K results ranked by similarity       │ │   │ │
│  │  └────────────────────────────────────────────────────┘ │   │ │
│  │                                                          │   │ │
│  │  ┌────────────────────────────────────────────────────┐ │   │ │
│  │  │ Keyword Search (Regex Pattern Matching)            │ │   │ │
│  │  │ 1. Search title & description with regex           │ │   │ │
│  │  │ 2. Return matching videos                          │ │   │ │
│  │  │ 3. Much faster than semantic                       │ │   │ │
│  │  └────────────────────────────────────────────────────┘ │   │ │
│  │                                                          │   │ │
│  │  ┌────────────────────────────────────────────────────┐ │   │ │
│  │  │ Hybrid Search (RECOMMENDED - Combines Both)        │ │   │ │
│  │  │ 1. Start with keyword search (fast filter)         │ │   │ │
│  │  │ 2. Rank results using semantic similarity          │ │   │ │
│  │  │ 3. Return top K best ranked results                │ │   │ │
│  │  │ 4. Best performance + accuracy balance             │ │   │ │
│  │  └────────────────────────────────────────────────────┘ │   │ │
│  └──────────────────────┬───────────────────────────────────┘   │ │
│                         │                                        │ │
│                         ↓                                        │ │
└──────────────────────────────────────────────────────────────────┤ │
│                                                                  │ │
│  External APIs                                                   │ │
│  ┌──────────────────────────────────────────────────────────┐  │ │
│  │  Google Generative AI (Gemini)                           │  │ │
│  │  - embedding-001 model for text embeddings              │  │ │
│  │  - Converts text to vector format (768 dimensions)      │  │ │
│  │  - Used for semantic similarity comparison              │  │ │
│  │  - Requires: GEMINI_API_KEY in .env                     │  │ │
│  └──────────────────────────────────────────────────────────┘  │ │
│                                                                  │ │
│  Database (MongoDB)                                              │ │
│  ┌──────────────────────────────────────────────────────────┐  │ │
│  │  Videos Collection                                       │  │ │
│  │  - title, discription, thumbnail, videoFile            │  │ │
│  │  - owner, duration, views, isPublished, etc.            │  │ │
│  └──────────────────────────────────────────────────────────┘  │ │
│                                                                  │ │
└──────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow - Hybrid Search Example

```
User Types: "JavaScript tutorial"
     ↓
SearchBar Component (Frontend)
     ↓
POST /api/v1/search/hybrid
{query: "JavaScript tutorial", limit: 10}
     ↓
     ┌─────────────────────────────────────────┐
     │ Search Controller Receives Request      │
     │ - Validates query length                │
     │ - Validates limit parameter             │
     └─────────────────────────────────────────┘
     ↓
     ┌─────────────────────────────────────────┐
     │ Hybrid Search Service                   │
     │                                         │
     │ STEP 1: Keyword Filter                  │
     │ ┌─────────────────────────────────────┐ │
     │ │ Search MongoDB:                     │ │
     │ │ - title: /javascript/i OR           │ │
     │ │ - title: /tutorial/i OR             │ │
     │ │ - description: /javascript/i OR     │ │
     │ │ - description: /tutorial/i          │ │
     │ │                                     │ │
     │ │ Result: 20 matching videos          │ │
     │ └─────────────────────────────────────┘ │
     │                                         │
     │ STEP 2: Semantic Ranking                │
     │ ┌─────────────────────────────────────┐ │
     │ │ For each of 20 videos:              │ │
     │ │ 1. Generate embedding for video    │ │
     │ │ 2. Calculate similarity with query │ │
     │ │ 3. Score: 0.0 - 1.0                │ │
     │ │                                     │ │
     │ │ Query embedding generated via       │ │
     │ │ Google Gemini API                   │ │
     │ │                                     │ │
     │ │ Results ranked by score             │ │
     │ └─────────────────────────────────────┘ │
     │                                         │
     │ STEP 3: Return Top 10                   │
     │ ┌─────────────────────────────────────┐ │
     │ │ Only return limit: 10 videos        │ │
     │ │ Sorted by similarity score descend  │ │
     │ └─────────────────────────────────────┘ │
     └─────────────────────────────────────────┘
     ↓
Response to Frontend:
{
  "statusCode": 200,
  "data": {
    "results": [
      {
        "_id": "...",
        "title": "Learn JavaScript - Full Tutorial",
        "discription": "...",
        "thumbnail": "...",
        "views": 1000,
        "owner": {...}
      },
      // ... 9 more results
    ],
    "count": 10,
    "searchType": "hybrid"
  }
}
     ↓
Frontend Updates State
     ↓
SearchResults Page Displays Videos
```

---

## Technology Stack

```
Frontend Layer:
├── React 18+
├── React Router (Navigation)
├── Axios (HTTP Client)
└── CSS (Styling)

Backend Layer:
├── Node.js
├── Express.js (Framework)
├── MongoDB (Database)
└── Mongoose (ODM)

AI/ML Layer:
├── Google Generative AI SDK
└── Gemini embedding-001 Model

Utils:
├── Custom Error Handling (ApiError)
├── Custom Response Format (ApiResponse)
└── Async Handler Middleware
```

---

## Key Features

✅ **Semantic Understanding**: Understands meaning, not just keywords
✅ **Fast Filtering**: Keyword search for initial filtering
✅ **Relevant Results**: Cosine similarity ranking
✅ **Scalable**: Works with any number of videos
✅ **Error Handling**: Graceful fallback to keyword search
✅ **Beautiful UI**: Animations and responsive design

---

## Performance Characteristics

```
Search Type     | Speed  | Accuracy | Use Case
─────────────────────────────────────────────
Keyword         | ⚡⚡⚡ | Good     | Exact matches
Semantic        | ⚡    | Excellent| Conceptual
Hybrid (Used)   | ⚡⚡ | Excellent| Best Balance
```

---

**Architecture Diagram Complete! 🎯**
